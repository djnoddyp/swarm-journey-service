swarm:
  logging:
    loggers:
      com.example.journeyservice.rest:
        level: DEBUG

  datasources:
    data-sources:
      MySQL:
        driver-name: mysql
        connection-url: jdbc:mysql://localhost:3306/swarmjourneydb?useSSL=false
        user-name: root
        password: password

#      H2:
#        driver-name: h2
#        connection-url: jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
#        user-name: sa
#        password: sa
